import { Controller,Get,Param,UseGuards } from '@nestjs/common'; import { ApiBearerAuth,ApiTags } from '@nestjs/swagger'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { RequirePermissions } from '../common/decorators/permissions.decorator'; import { PermissionsGuard } from '../common/guards/permissions.guard'; import { TenantAccessGuard } from '../common/guards/tenant-access.guard'; import { PrismaService } from '../database/prisma.service';
@ApiTags('Organizations') @ApiBearerAuth() @Controller({path:'choirs/:choirId/organization',version:'1'}) @UseGuards(JwtAuthGuard,TenantAccessGuard,PermissionsGuard) export class OrganizationsController {constructor(private prisma:PrismaService){} @Get() @RequirePermissions('organization.read') get(@Param('choirId')choirId:string){return this.prisma.organization.findFirst({where:{choirs:{some:{id:choirId}}},select:{id:true,name:true,type:true,status:true,createdAt:true,choirs:{select:{id:true,name:true,slug:true,timezone:true,status:true}}}})}}
