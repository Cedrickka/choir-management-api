import { Body,Controller,Get,Param,Post,UseGuards } from '@nestjs/common'; import { ApiBearerAuth,ApiTags } from '@nestjs/swagger'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { RequirePermissions } from '../common/decorators/permissions.decorator'; import { PermissionsGuard } from '../common/guards/permissions.guard'; import { TenantAccessGuard } from '../common/guards/tenant-access.guard'; import { AssignRoleDto } from './dto/assign-role.dto'; import { CreateRoleDto } from './dto/create-role.dto'; import { RolesService } from './roles.service';
@ApiTags('Organizations') @ApiBearerAuth() @Controller({path:'choirs/:choirId/roles',version:'1'}) @UseGuards(JwtAuthGuard,TenantAccessGuard,PermissionsGuard) export class RolesController {constructor(private roles:RolesService){} @Get() @RequirePermissions('roles.read') list(@Param('choirId')c:string){return this.roles.list(c)} @Post() @RequirePermissions('roles.manage') create(@Param('choirId')c:string,@Body()d:CreateRoleDto){return this.roles.create(c,d)} @Post('memberships/:membershipId') @RequirePermissions('roles.assign') assign(@Param('choirId')c:string,@Param('membershipId')m:string,@Body()d:AssignRoleDto){return this.roles.assign(c,m,d.roleId)}}
